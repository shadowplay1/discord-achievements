export interface ILoggerColors {
    red: string
    green: string
    yellow: string
    blue: string
    magenta: string
    cyan: string
    white: string
    reset: string
    black: string
    lightgray: string
    default: string
    darkgray: string
    lightred: string
    lightgreen: string
    lightyellow: string
    lightblue: string
    lightmagenta: string
    lightcyan: string
}


/**
 * @typedef {object} ILoggerColors Console colors object.
 * @prop {string} red Red color.
 * @prop {string} green Green color.
 * @prop {string} yellow Yellow color.
 * @prop {string} blue Blue color.
 * @prop {string} magenta Magenta color.
 * @prop {string} cyan Cyan color.
 * @prop {string} white White color.
 * @prop {string} reset Reset color.
 * @prop {string} black Black color.
 * @prop {string} lightgray Light gray color.
 * @prop {string} default Default color.
 * @prop {string} darkgray Dark gray color.
 * @prop {string} lightred Light red color.
 * @prop {string} lightgreen Light green color.
 * @prop {string} lightyellow Light yellow color.
 * @prop {string} lightblue Light blue color.
 * @prop {string} lightmagenta Light magenta color.
 * @prop {string} lightcyan Light cyan color.
 */
